{
  "name": "Quick Count",
  "secrets": [
    {
      "key": "APP_PUBKEY",
      "description": "The application's receiving wallet address on the Usernode blockchain network. All vote submission transactions are sent to this address.",
      "required": true,
      "private": false
    },
    {
      "key": "IPFS_API_URL",
      "description": "IPFS pinning service API endpoint (e.g. https://api.pinata.cloud). Used to store photo evidence for vote submissions.",
      "required": true,
      "private": false,
      "default": "https://api.pinata.cloud"
    },
    {
      "key": "IPFS_API_KEY",
      "description": "API key for the IPFS pinning service. Required to upload photo evidence files.",
      "required": true,
      "private": true,
      "staging_default": "staging-test-key"
    }
  ],
  "tests": [
    { "name": "App shell loads", "path": "/", "expectSelector": "h1" },
    { "name": "Dashboard renders", "path": "/", "expectSelector": "#dashboard" },
    { "name": "Stations table renders", "path": "/", "expectSelector": "#stations-table" },
    { "name": "Explorer renders", "path": "/explorer", "expectSelector": "#tx-list" }
  ]
}
