{
  "name": "Quick Count",
  "secrets": [
    {
      "key": "NODE_RPC_URL",
      "description": "Base URL of the Usernode node the indexer polls for on-chain transactions. Platform-managed in-network; optional for standalone deploys (the indexer no-ops when unset).",
      "required": false,
      "private": false
    },
    {
      "key": "APP_PUBKEY",
      "description": "On-chain wallet address for receiving result submissions. Leave blank to enable LOCAL_DEV mode.",
      "required": false,
      "private": false,
      "default": ""
    },
    {
      "key": "LOCAL_DEV",
      "description": "Set to false in production to enable live chain calls. When true (default), all blockchain operations are stubbed.",
      "required": false,
      "private": false,
      "default": "true"
    }
  ],
  "tests": [
    {
      "name": "Election dashboard loads",
      "path": "/",
      "expectText": "Staging Demo General Election"
    },
    {
      "name": "Dashboard shows station count",
      "path": "/",
      "expectText": "stations reported"
    },
    {
      "name": "Transaction explorer renders",
      "path": "/#explorer",
      "expectSelector": "#screen-explorer"
    },
    {
      "name": "Candidate tally visible",
      "path": "/",
      "expectText": "Alpha"
    }
  ]
}
